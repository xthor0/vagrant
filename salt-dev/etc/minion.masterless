hash_type: sha256

# One of: all, garbage, trace, debug, profile, info, warning, error, critical, quiet.
log_level_logfile: debug

file_client: local

fileserver_backend:
   - roots

#state_verbose: False
state_verbose: True

file_roots:
  base:
    - /srv/salt/states

pillar_roots:
  base:
    - /srv/salt/pillars
